{
  "installCommand": "cd ../.. && yarn install --frozen-lockfile",
  "buildCommand": "cd ../.. && yarn build:types && yarn build:utils && yarn build:storybook && yarn build:admin",
  "outputDirectory": "dist",
  "framework": "vite",
  "devCommand": "yarn dev",
  "regions": ["iad1"],
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
